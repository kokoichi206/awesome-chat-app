package presentation.ui

import androidx.compose.ui.graphics.Color

object AppColors {
    val ButtonBlue = Color(0xFF1D00F6)
    val LightGray = Color.Gray.copy(alpha = 0.2f)
    val DarkGray = Color.Gray.copy(alpha = 0.7f)
}
