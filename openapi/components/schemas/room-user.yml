type: object
description: ユーザー（他者）の情報
properties:
  id:
    description: ユーザー ID
    type: string
    format: uuid
  img_url:
    description: アイコン画像の URL
    type: string
  username:
    description: 表示名
    type: string
  last_read_at:
    description: そのルームの内容を最後に確認した日時（ISO 8601 形式）
    type: string
required:
  - id
  - username
example:
  $ref: "../examples/room-user.yml"
